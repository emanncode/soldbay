# Day Work — 2026-07-26

Full log of work completed on Soldbay (`soldbay-web` + `soldbay-app`). Paths are relative to the project root unless noted.

---

## 1. Seller Dashboard Screen — Expo App (`soldbay-app`)

### Goal

Build a seller dashboard that shows wallet balance, listing grid, and handles empty/verification states.

### Files created

| File | Summary |
|---|---|
| `soldbay-app/src/app/seller/dashboard.tsx` | Full dashboard screen (~480 lines). Sections: top bar (tag-icon logo + "Seller Dashboard" text + avatar initial circle), wallet balance card (GlassPanel `panel-focus` variant, formatted amount with `₦` + comma separators, "Request payout →" link that shows toast), "Your listings" section header + red pill "+ Add listing" button, listing grid (thumbnail or placeholder, title, price with `TagIcon`, status badge — Active=green, Sold out=amber, Inactive=gray), empty state ("You haven't listed anything yet" + "Add your first listing" button inside GlassPanel). Verification guard: if `verifiedAt` is null, redirects to `/seller/verify`. Auto-dismissing toast for "coming soon" actions. |

### Files modified

| File | Change |
|---|---|
| `soldbay-app/src/app/_layout.tsx` | Added `<Stack.Screen name="seller/dashboard" />` |
| `soldbay-app/src/app/login.tsx` | After login, sellers route to `/seller/dashboard`, buyers route to `/` |
| `soldbay-app/src/app/seller/verify.tsx` | "Verified" state now shows a "Go to dashboard" `PrimaryButton` that navigates to `/seller/dashboard` |

---

## 2. Backend API — Extend GET /api/sellers/me (`soldbay-web`)

### Goal

Return enough data for the dashboard to render without extra API calls: seller name, wallet balance, and all listings with categories/images/stock/status.

### Files modified

| File | Change |
|---|---|
| `soldbay-web/src/app/api/sellers/me/route.ts` | Extended `GET` response to include `name` (from related User), `walletBalance` (from SellerProfile), and `listings[]` array (each with id, title, price, images, stock, status, createdAt, category name). |

---

## 3. Mobile API Types (`soldbay-app`)

### Goal

Type the new backend response so the dashboard can access `name`, `walletBalance`, and `listings` with full type safety.

### Files modified

| File | Change |
|---|---|
| `soldbay-app/src/lib/api.ts` | Added `SellerListing` type (id, title, price, images, stock, status, createdAt, category). Extended `SellerMeResponse` with `name`, `walletBalance`, `listings[]`. |

---

## 4. UI Component — Disabled State (`soldbay-app`)

### Goal

Prevent user interaction on form fields and role cards during loading/submission.

### Files modified

| File | Change |
|---|---|
| `soldbay-app/src/components/glass-form-field.tsx` | Added `disabled` prop. When true: dims opacity to 0.5, sets `editable={false}`, `selectTextOnFocus={false}`. |
| `soldbay-app/src/components/role-card.tsx` | Added `disabled` prop. When true: dims opacity to 0.5, ignores `onPress`. |

---

## 5. Form Screens — Disable During Submission (`soldbay-app`)

### Goal

Lock all form inputs and buttons while a request is in flight to prevent double-submits and provide visual feedback.

### Files modified

| File | Change |
|---|---|
| `soldbay-app/src/app/login.tsx` | Added `disabled={loading}` to email and password `GlassFormField` |
| `soldbay-app/src/app/signup.tsx` | Added `disabled={loading}` to all 4 `GlassFormField` (name, email, password, confirm) and both `RoleCard` |
| `soldbay-app/src/app/forgot-password/index.tsx` | Added `disabled={saving}` to email `GlassFormField` |
| `soldbay-app/src/app/forgot-password/new-password.tsx` | Added `disabled={saving}` to code and new password `GlassFormField` |

---

## 6. Routing Changes

### Login flow

```
Login (seller) → /seller/dashboard  (if verifiedAt != null)
               → /seller/verify     (if verifiedAt == null)

Login (buyer)  → /
```

### Signup flow

```
Signup → /select-university → PATCH universityId → /seller/verify → (on success) /seller/dashboard
```

### Files changed

| File | Routing logic |
|---|---|
| `soldbay-app/src/app/login.tsx` | `router.replace(res.user.role === "SELLER" ? "/seller/dashboard" : "/")` |
| `soldbay-app/src/app/seller/verify.tsx` | "Go to dashboard" button: `router.replace("/seller/dashboard")` |

---

## 9. Seller Dashboard — Pencil Design Alignment

### Goal

Match the existing seller dashboard screen to the approved Pencil design (`design/starter.pen`). The verify screen already matched and required no changes.

### Files modified

| File | Change |
|---|---|
| `soldbay-app/src/app/seller/dashboard.tsx` | Replaced `TagIcon` logo mark with `logo2.png` image (32x32). Changed avatar from solid red (`#e1261c`) to transparent (`#ffffff1a`) with white border (`#ffffff1f`). Wallet label color `#ffffff99` → `#ffffff80`. Removed `→` arrow from "Request payout", color `#ffffff66` → `#ffffff33`. Section title fontSize 20 → 22. "Add listing" button: replaced `Ionicons add` with `+` text. Empty state icon: `pricetags-outline` → `cube-outline`. Empty state: fixed height 300 with centered content. Listing price: replaced `TagIcon` component with `Ionicons pricetag`. |
| `soldbay-app/assets/logo2.png` | Logo without "soldbay" text (new asset) |
| `soldbay-web/public/logo2.png` | Same logo for web (new asset) |

### Design tokens verified against Pencil

| Element | Design | Code |
|---|---|---|
| Logo mark | `logo2.png`, 32x32, cornerRadius 8 | ✅ |
| Avatar | `#ffffff1a` fill, `#ffffff1f` stroke, 36x36 | ✅ |
| Wallet label | `#ffffff80`, Inter 13, weight 500 | ✅ |
| Wallet amount | `#ffffff`, Bricolage Grotesque 32, weight 700 | ✅ |
| Request payout | `#ffffff33`, Inter 13, weight 500 | ✅ |
| Section title | `#ffffff`, Bricolage Grotesque 22, weight 600 | ✅ |
| Add listing button | `+` text + "Add listing", red pill | ✅ |
| Empty state icon | `cube-outline` (package), `#ffffff33` | ✅ |
| Empty state | 300px height, centered | ✅ |

---

## 13. Buyer Home Screen — Expo App (`soldbay-app`)

### Goal

Build the buyer landing screen with search, category filtering, and a 2-column listing grid. This is the first screen buyers see after login or signup + university selection.

### Files created

| File | Summary |
|---|---|
| `soldbay-app/src/app/buyer/home.tsx` | Full buyer home screen (~300 lines). Top bar with `logo2.png`, search icon, notification bell. Custom search input (`TextInput` with search icon, "Search for anything on campus", client-side title filtering). Horizontal scrolling category chips ("All" + categories from `GET /api/categories`, active = red tint, inactive = transparent). "Trending near you" section header. 2-column grid of listing cards (thumbnail 100px or placeholder, title 12px, price with tag icon in red, `@username · Category` line). Empty state with package icon, contextual message, "Browse all" pill button when category filter active. |

### Files modified

| File | Change |
|---|---|
| `soldbay-app/src/lib/api.ts` | Added `PublicListing` type (with `seller: { username, businessName }`, `category: { name, slug }`), `Category` type, `getListings(categorySlug?)` function, `getCategories()` function |
| `soldbay-app/src/app/_layout.tsx` | Registered `buyer/home` screen |
| `soldbay-app/src/app/login.tsx` | Buyers now route to `/buyer/home` (was `/`) |
| `soldbay-app/src/app/select-university.tsx` | After university selection, fetches `getMe()` to check role — sellers → `/seller/verify`, buyers → `/buyer/home` |

---

## 14. API Types — Listings & Categories (`soldbay-app`)

### Goal

Type the public listing and category endpoints so the buyer home screen can consume them with full type safety.

### Files modified

| File | Change |
|---|---|
| `soldbay-app/src/lib/api.ts` | Added `PublicListing` interface (id, sellerId, categoryId, title, description, price, images, stock, status, createdAt, seller { username, businessName }, category { name, slug }). Added `Category` interface (id, name, slug, commissionRate). Added `getListings(categorySlug?)` and `getCategories()` functions. |

---

## 15. Routing — Buyer Role (`soldbay-app`)

### Goal

Ensure buyers land on `/buyer/home` after login or signup + university selection, not on any seller-specific screen.

### Updated routing

```
Login (buyer)  → /buyer/home
Login (seller) → /seller/dashboard

Signup (buyer)  → /select-university → /buyer/home
Signup (seller) → /select-university → /seller/verify
```

### Files changed

| File | Routing logic |
|---|---|
| `soldbay-app/src/app/login.tsx` | `router.replace(res.user.role === "SELLER" ? "/seller/dashboard" : "/buyer/home")` |
| `soldbay-app/src/app/select-university.tsx` | After PATCH, fetches `getMe()` → `me.role === "SELLER" ? "/seller/verify" : "/buyer/home"` |

---

## 16. Commits (all)

| Hash | Message |
|---|---|
| `a95b10a` | `feat(api): extend GET /api/sellers/me with name, walletBalance, and listings` |
| `9a18c11` | `feat(app/api): add SellerListing type and extend SellerMeResponse with name, walletBalance, listings` |
| `f7d93fd` | `feat(app/ui): add disabled prop to GlassFormField and RoleCard` |
| `a9e6c4d` | `feat(app/forms): disable form fields during loading on signup and forgot-password` |
| `2fb6a94` | `feat(app/seller): add seller dashboard screen and wire login/verify redirects` |
| `09ab95c` | `feat(app/seller): add seller dashboard screen, register in layout, add dashboard link on verification success` |
| `30fdc90` | `ui(dashboard): match Pencil design — logo2, transparent avatar, updated colors and icons` |
| `c54680b` | `feat(app/api): add PublicListing and Category types, add getListings and getCategories functions` |
| `87dcb88` | `feat(app): register buyer/home screen in root layout` |
| `b0fde03` | `feat(app/auth): route buyers to /buyer/home after login` |
| `c16e4f0` | `feat(app/auth): fetch role after university selection and route accordingly` |
| `169df81` | `chore(app/seller): formatting cleanup in dashboard` |
| `c48c105` | `feat(app/buyer): add buyer home screen with search, category filter, and listing grid` |
| `c19f028` | `feat(app/splash): add animated splash screen with logo2 drop, scale, shift left, and text reveal` |
| `5f95390` | `feat(app/splash): logo2 drops, scales, shifts left, 'soldbay' types out letter-by-letter` |

---

## 21. Files touched (complete list — all work)

### Created

| File | Purpose |
|---|---|
| `soldbay-app/src/app/seller/dashboard.tsx` | Seller dashboard screen |
| `soldbay-app/src/app/buyer/home.tsx` | Buyer home screen (search, categories, listing grid) |
| `soldbay-app/assets/logo2.png` | Logo without "soldbay" text |
| `soldbay-web/public/logo2.png` | Same logo for web |

### Modified

| File | What changed |
|---|---|
| `soldbay-web/src/app/api/sellers/me/route.ts` | Returns name, walletBalance, listings |
| `soldbay-app/src/lib/api.ts` | SellerListing, PublicListing, Category types; getListings(), getCategories() |
| `soldbay-app/src/components/glass-form-field.tsx` | disabled prop |
| `soldbay-app/src/components/role-card.tsx` | disabled prop |
| `soldbay-app/src/app/_layout.tsx` | Registered seller/dashboard, buyer/home screens |
| `soldbay-app/src/app/login.tsx` | Seller/buyer routing + disabled fields |
| `soldbay-app/src/app/signup.tsx` | disabled fields |
| `soldbay-app/src/app/forgot-password/index.tsx` | disabled field |
| `soldbay-app/src/app/forgot-password/new-password.tsx` | disabled fields |
| `soldbay-app/src/app/seller/verify.tsx` | "Go to dashboard" button |
| `soldbay-app/src/app/seller/dashboard.tsx` | Pencil design alignment (logo2, avatar, colors, icons) |
| `soldbay-app/src/app/select-university.tsx` | Buyer-aware routing after university selection |
| `soldbay-app/src/components/splash-screen.tsx` | Animated splash: logo2 drops, scales, shifts left, "soldbay" types out letter-by-letter (purple "bay") |

---

## 22. Open Items

- **Buyer listing detail** — tap a listing card → no navigation yet (needs detail screen)
- **Buyer cart / checkout** — no screen or API yet
- **Buyer profile** — no screen yet
- **Notification bell** — currently no-op (no notifications API)
- **Search icon in buyer home** — toggles search mode but no full search screen
- **"+ Add listing" button** (seller dashboard) — shows "Create listing coming soon" toast
- **"Request payout"** (seller dashboard) — shows "Payout coming soon" toast
- **Dashboard listing thumbnails** — placeholder icon for all; wire real image URLs when `images[]` populated
- **`select-university` buyer flow** — fetches `getMe()` after PATCH to route; could cache role to avoid extra request
- **Logout button** — needs implementation
