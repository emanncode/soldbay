# Day Work — 2026-07-22

Full log of work completed on Soldbay web (`soldbay-web`). Paths below are relative to `soldbay-web/` unless noted.

---

## 1. Logo click closes sidebar on mobile

### Problem

On mobile, clicking the logo to navigate home did not close the open sidebar menu. Only the Buyer/Seller links had `onClick={() => setMenuOpen(false)}`.

### Fix

Added `onClick={() => setMenuOpen(false)}` to the logo `<Link>` in `site-nav.tsx:68`. Now clicking the logo navigates to `/` and closes the mobile sidebar.

### Files modified

| File | Change |
|---|---|
| `src/components/site-nav.tsx` | Added `onClick={() => setMenuOpen(false)}` to the home logo link |

---

## 2. Fix hydration mismatch in join form

### Problem

Browser hydration warnings on `/join/buyer` showed className mismatches between server and client rendered `<input>` elements. The `Input` UI component's base classes (`h-10`, `border-none`, `rounded-2xl`, etc.) conflicted with the form-specific `fieldClass` (`h-12`, `border`, `rounded-xl`, etc.) when merged via `cn()` + `tailwind-merge`. The merge produced different class output between server and client bundles.

### Fix

Replaced the three `<Input>` usages in `join-form.tsx` with plain `<input>` elements using `fieldClass` directly, bypassing the `cn()` merge entirely. Also added explicit `type="text"` to the name and sellCategory inputs (fixing a `type={undefined}` issue).

### Files modified

| File | Change |
|---|---|
| `src/components/join-form.tsx` | Replaced `<Input>` with `<input>` for name, email, and sellCategory fields; removed unused `Input` import; added explicit `type` props |

---

## 3. Mobil login/signup app (`soldbay-app`)

### Goal

React Native / Expo mobile app matching the Pencil design (`design/starter.pen`): dark glassmorphic Login + Signup screens, logo, buyer/seller role selection, password visibility, confirm password validation.

### Files created

| File | Summary |
|---|---|
| `soldbay-app/` (project root) | Expo SDK 57, NativeWind v4, `@expo/vector-icons`, Bricolage Grotesque + Inter fonts, `expo-router` file-based routing |
| `soldbay-app/src/app/index.tsx` | Root redirect → `/login` |
| `soldbay-app/src/app/login.tsx` | Login screen: email + password fields, eye toggle (Ionicons), PrimaryButton, link to `/signup` |
| `soldbay-app/src/app/signup.tsx` | Signup screen: name, email, password, confirm password; role selector cards (Buyer/Seller); disclaimer (Buyer only); link to `/login` |
| `soldbay-app/src/components/logo-wordmark.tsx` | Logo from `assets/logo.png` (copied from `soldbay-web/public/logo.png`) |
| `soldbay-app/src/components/tag-icon.tsx` | SVG tag icon (background stamp) |
| `soldbay-app/src/components/glass-form-field.tsx` | Reusable filed with label, input, optional right element, error state, glass styling |
| `soldbay-app/src/components/primary-button.tsx` | Red pill button with loading spinner, disabled state |
| `soldbay-app/src/components/role-card.tsx` | Role selector: 3px border, glass default, red-tinted shadow on selected |
| `soldbay-app/src/components/glass-panel.tsx` | Frosterd card with `BlurView` (intensity 30, dark tint), inset highlight, border |

### Design sync

- Auth screens done: Login + Signup (Buyer/Seller variant)
- Pencil `design/starter.pen` used as reference for all visual details (glass fill, border, glow, spacing, role cards, error states)

### Files modified

| File | Change |
|---|---|
| `soldbay-app/package.json` | Added `@expo/vector-icons` dependency |
| `soldbay-app/tsconfig.json` | Added `"exclude": ["example/"]` |
| `soldbay-app/src/app/_layout.tsx` | Registered stack screens (index, login, signup), hid header |
| `soldbay-app/src/app/index.tsx` | Replaced default template with `<Redirect href="/login" />` |
| `soldbay-app/src/app/dev-glass-test.tsx` | Updated imports to use `@/` path alias |
| `soldbay-app/src/components/page-atmosphere.tsx` | Added `position: 'relative'` for proper stacking context |
| `soldbay-app/src/app/login.tsx` | Replaced manual glass card View with `<GlassPanel variant="panel">` for background blur |
| `soldbay-app/src/app/signup.tsx` | Same — replaced manual View with `<GlassPanel>` |

### Bug fixes

- `npm install` fixed missing `@expo/metro-runtime` and `expo` module resolution errors
- `npx expo install expo-modules-core` fixed `expo-modules-core` resolution error
- Clean rebuild with `--clear` verifies all 6 routes bundle: `/`, `/login`, `/signup`, `/_sitemap`, `/+not-found`, `/dev-glass-test`

### Open / next

Same as previous: school-email restriction, student ID verification, seed universities, Neon cold-start keep-alive, hero art. Plus: wire mobile-login API, store Bearer token on device.
